<%= form_for student do  |f| %>
  <%= f.label(:name) %>
  <%= f.text_field(:name, class: 'input-group') %>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <%= f.label :dob %>
  <%= f.date_field :dob %>

  <%= f.submit 'Create a cool student!' %>

<% end %>
